Purpose: Create a minimal FastAPI project scaffold with a single example endpoint, tests, and README.

Constraints:
- Keep code in the subfolder `generated_project/` (do not modify repository root files).
- No secrets or credentials in code.

Acceptance Criteria:
- A working `app/main.py` with a GET `/` endpoint returning status.
- `requirements.txt`, `README.md`, and a `features/health_check.feature` test (behave) included.
- Git repo initialized inside `generated_project/` with an initial commit.
