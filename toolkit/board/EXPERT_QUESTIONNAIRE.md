# Expert Questionnaire: Agent Structure Research

Please answer the following before or during the Board review meeting.

1) Which storage model do you prefer for agent/toolkit artifacts? (flat, per-agent bundle, hybrid, separate repo)

2) For agent-specific templates and rules, do you prefer per-agent subfolders or shared templates with namespacing?

3) Any platform constraints? (GitHub-only, GitLab-only, multi-platform) — how should that affect placement?

4) For agent permissions, should we allow `allow_direct_commits: true` in any cases? If yes, which controls are required?

5) Recommended minimum checks that must pass before an agent-generated PR is opened (format/lint/tests/security):

6) Retention policy suggestions for reports and audits (how long to keep `toolkit/reports/` and `toolkit/audits/`?):

7) Other approaches or tool integrations we should consider (e.g., policy-as-code frameworks, CAF, in-repo policy engines):

8) Any references or examples from your experience or open-source projects we should review?
